package file_example;
typestate Protocol {

	session
		Init =		{
					Status open(): Open
				}
	where
		Open =		{
					BooleanEnum hasNext(): Close,
					void read(): Close
				}
	    
		Close =		{
					void close(): end
				}
}
