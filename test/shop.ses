package test;
import test.MyObject;

typestate MyShop {
	session
			Start =	Init

	where
			Init =	{
					void init(): Shop
				}

			Shop =	{
			                int price(): Shop,
					Choice buy(MyObject <{void buyme(): {}}>, int): <ok: Pay, error: Shop>,
					void stop(): {},
					int lol():end
				}

			Pay =	{
					int pay(): Shop
				}

}
