package test;
import test.mytypestate.*;
//import test.*;
class Cases {
	number n = new number(number.ena);

	static void main(MyObject d) {
		MyObject a = new MyObject();
		int c;
//		switch(a.s.s.s.s.s.ee) {
//		switch(test.mytypestate.MyObject.s) {
		switch(n.getEnum()) {
//		switch(a.foo(null)) {
//		switch(a.foo(1, 'g')) {
			case number.ena:
			case number.dio:
				//test.MyObject = test.mytypestate.MyObject.s;
			default:
		}
	}

	MyObject foo() {}
}
