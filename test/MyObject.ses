package test;
class MyObject typestate ABCD {
	private MyObject s;
	Choice cc;
	number e;

	MyObject foo(MyObject aa) {
		MyObject s = new MyObject();
		s.foo(new My());
		s = new MyObject();
		s.foo(new My());
		number e = new number(number.dio);
	}

	int foo(My a) {}
}
