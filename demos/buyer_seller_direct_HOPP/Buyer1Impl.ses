package demos.buyer_seller_direct_HOPP;
import java.io.*;

public class Buyer1Impl typestate Buyer1 {
    private PrintWriter socketSeller = null;
    private PrintWriter socketBuyer2 = null;

    private int balance;

    public Buyer1Impl () {
    	socketSeller.println("Brave New World");
    }

    public void price (int amount) {
        balance = (int)(0.4 * amount);
    	buyer2.quote(amount - balance);
        socketBuyer2.println(quote);      
    }

    public void agree () {
        socketSeller.println(balance);
    }

    public void quit () {
    }
}
