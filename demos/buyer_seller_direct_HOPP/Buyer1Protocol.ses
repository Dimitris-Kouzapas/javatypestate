package demos.buyer_seller_direct_HOPP;

typestate Buyer1Protocol {
	session
		Init = {
			void price(int): AwaitingAgreement
		}
	where
		AwaitingAgreement = {
		 	void agree (): end,
			void quit (): end
		}
}
