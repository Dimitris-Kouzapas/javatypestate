package demos.buyer_seller.direct;

typestate Buyer1 {
	session
		Init = {
			Exit_price_request init(): <PRICE_RESPONSE: Awaiting_price_response, OUT_OF_STOCK: end>
		}
	where
		Awaiting_price_response = {
			void price_response(int): end
		}
}
