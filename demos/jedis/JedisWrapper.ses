package demos.jedis;

import java.util.List;

// Need to be able to specify classpath as a command-line argument.
// import redis.clients.jedis.JedisCommands;

// Couple of significant obstacles to further experimentation:
// 1) Can't compile against a third-party library (we only compile .ses to .java)
// 2) Based on a J2SE 1.4 front end, so we don't support any post-2002 language features.

public class JedisWrapper typestate Commands {
    void watch(String[] keys) {
    }

    String get(String keys) {
       throw new RuntimeException();
    }

	List/*<String>*/ lrange() {
       throw new RuntimeException();
	}
	
	void multi () {
	}

    void set(String key, String value) {
    }

    String lpop (String key) {
       throw new RuntimeException();
    }
    
    void discard () {
    }
    
    Result exec () {
       throw new RuntimeException();
    } 
}
