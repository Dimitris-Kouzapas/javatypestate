package demos.client_server;
import java.io.*;

public class Client typestate ClientProtocol {
    private BufferedReader serverIn;
    private PrintWriter serverOut;
    
    public Client () {
    	// initialise socket
    }

	void sendRequest(int k) {
		// send k on server out
	}
	
	String receiveResponse() {
		String v = "hello"; // wait for v on serverIn
		return v;
	}
	
	// We drive the interaction from the "outside". The object itself just represents the channel
	// and has no behaviour as such.
	public static void main (String[] args) {
		Client client = new Client();
		client.sendRequest(5);
		String v = client.receiveResponse();
	}
}
