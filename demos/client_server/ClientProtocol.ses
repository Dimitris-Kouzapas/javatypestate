package demos.client_server;

// This is roughly what we currently generate from the local projection.
typestate ClientProtocol {
	session
		Init = {
			void sendRequest(int): AwaitingResponse
		}
	where
		AwaitingResponse = {
			String receiveResponse(): end
		}
}
