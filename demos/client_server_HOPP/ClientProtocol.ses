package demos.client_server_HOPP;

// We generate this by considering only "from" messages in the local projection.
// No need to include "receive" in the generate method name as that is implied.
typestate ClientProtocol {
	session
		Init = {
			void response(String): end
		}
}
