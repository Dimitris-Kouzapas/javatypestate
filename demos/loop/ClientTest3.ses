package demos.loop;

class ClientTest1 {
	void test2() {
		test(new LoopImpl());
	}

  	void test (LoopImpl loop) {
//  		LoopImpl loop = new LoopImpl();
		switch(loop.finished().getEnum()) {
			case Bool.FALSE:
				test(loop);
			case Bool.TRUE:
				break;
		}
  	}
}
